[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fire-code-embr"
version = "0.1.0"
description = "A small, provenance-aware Fire Code and Embr compiler pipeline."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Alana Sky" }]
license = { text = "MIT" }
